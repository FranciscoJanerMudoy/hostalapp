import 'package:flutter/material.dart';

class InfoOrderScreen extends StatelessWidget {
  const InfoOrderScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Información comanda'),
        centerTitle: true,
      ),
      body: Column(),
    );
  }
}
